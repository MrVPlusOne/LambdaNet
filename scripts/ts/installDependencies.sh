#!/usr/bin/env bash
npm install -g \
typescript \
cloc

npm i fs source-map-support yargs \
@types/node @types/source-map-support @types/typescript @types/yargs
# remember to compile the scripts under scripts/ts using tsc
